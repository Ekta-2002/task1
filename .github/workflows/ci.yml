name: To test the workflow

on:
  push:

jobs:                     # Collection of jobs
  build:
    runs-on: ubuntu-latest   # Runner (OS)

    steps:                   # Steps inside job
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run a script
        run: echo "Hello, GitHub Actions!"
  
